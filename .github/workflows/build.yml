name: Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build macOS App
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Show Xcode version
        run: |
          xcodebuild -version
          xcode-select -p
      
      - name: Build app
        run: |
          cd HealthReminder
          xcodebuild \
            -project HealthReminder.xcodeproj \
            -scheme HealthReminder \
            -configuration Release \
            -derivedDataPath ./build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            build
      
      - name: Check build output
        run: |
          echo "✅ Build completed successfully!"
          ls -la HealthReminder/build/Build/Products/Release/
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: HealthReminder-Build-${{ github.sha }}
          path: HealthReminder/build/Build/Products/Release/HealthReminder.app
          retention-days: 7
